// keys, values, entries

const producto = {
  nombre: "Monitor 20 pulgadas",
  precio: 200,
  disponible: true,
  };

console.log(Object.keys(producto)); //solo las keys
console.log(Object.values(producto)); // solo los valores
console.log(Object.entries(producto)); // cada key/value como array
